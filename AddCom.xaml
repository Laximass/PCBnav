<Window x:Class="PCB_Nav3.AddCom"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Comment" Height="300" Width="400">

    <Grid x:Name="AddComGrid" Background="{DynamicResource BGColour}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label x:Name="PartNumlbl" Content="Part Number:" Grid.Row="0"
               Foreground="{DynamicResource TextColour}" Margin="10"/>
        <TextBox x:Name="PartNumComtb" Grid.Row="0" Margin="100,0,10,0"
                 Text="{Binding PartNumber, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 Foreground="{DynamicResource TextColour}"/>

        <Label x:Name="Commentlbl" Content="Comment:" Grid.Row="1"
               Foreground="{DynamicResource TextColour}" Margin="10"/>
        <TextBox x:Name="CommentComtb" Grid.Row="1" Margin="100,0,10,0"
                 Text="{Binding Comment, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True" Height="100"
                 Foreground="{DynamicResource TextColour}"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button Content="OK" Command="{Binding SaveCommentCommand}" Margin="5"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
